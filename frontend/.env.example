# Base URL of the CPR Medical Academy backend API.
# TODO: point at the real API once the backend is deployed.
VITE_API_BASE_URL=http://localhost:8000/api

# WhatsApp number used by the sticky chat bubble (international format, no +).
VITE_WHATSAPP_NUMBER=8801700000000
