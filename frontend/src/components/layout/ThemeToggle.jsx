import { useTheme } from '@/hooks/useTheme';
import { cn } from '@/lib/utils';

/** Light/dark switch backed by the Zustand theme store. */
export default function ThemeToggle({ className }) {
  return null;
}
